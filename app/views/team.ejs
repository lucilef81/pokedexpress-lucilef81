<%- include('header') %>

  <% if(pokemons.length == 0) { %>
    Aucun pokemon dans votre équipe !
  <% } %>

  <% if(locals.error) { %>
    <div class="row">
        <%=error%>
    </div>
  <% } %>
    
  <% for(let pokemon of pokemons) { %>
    <div class="thumbnail">
      <a href="/pokemon/<%= pokemon.numero %>">
        <img src="/img/<%= pokemon.numero %>.png" alt="Illustration de <%= pokemon.nom %>">
        <span><%= pokemon.nom %></span>
      </a>
      <div>
        <a class="link--delete" href="/team/delete/<%= pokemon.numero %>">Supprimer</a>
      </div>
    </div>
  <% } %>

<%- include('footer') %>